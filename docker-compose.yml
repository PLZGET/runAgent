services:
  backend:
    build:
      context: ./backend
    ports:
      - "8002:8000"

  frontend:
    build:
      context: ./frontend
    environment:
      BACKEND_INTERNAL_URL: http://backend:8000
    ports:
      - "3002:3002"
    depends_on:
      - backend
